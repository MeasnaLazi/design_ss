# Copy to `.env` in this directory (`agent_toolkit/`, next to `pyproject.toml`) and adjust if needed.
# Used by `python -m agent_toolkit designer …`.
# Must stay on loopback (localhost / 127.0.0.1 / ::1); see `src/agent_toolkit/designer_client.py`.

DESIGNER_API_BASE=http://localhost:4713/__api/screenshot-designer
