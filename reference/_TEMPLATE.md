---
few_shot_id: CHANGE_ME_SLUG
title: CHANGE_ME_SHORT_TITLE
use_for: screenshot_planning
content_type: layout_description
# optional:
# related_brief_sections: creative_plan.panels
# notes: One-line scope or non-goals.
---

# CHANGE_ME_SHORT_TITLE

## Overview

Who this pattern is for and what problem it solves — **without** naming specific app copy or on-screen strings.

## Strip and background

Column count, aspect intent, background treatment, contrast, gutters, continuity across the strip.

## Text layers

Marketing (or caption) blocks: placement, count per column, hierarchy, wrapping, margins relative to the column, relationship to safe zones and devices.

## Device frames

Hardware read, count and density, pose and tilt, scale mix, stacking and z-order, cross-column continuation, screen **roles** only (what kind of UI, not literal text).

## Invariants

Rules that separate **text** behavior from **device** behavior (overlap, clipping, gutters, occlusion).

## Composition notes

Optional: variety expectations, anti-patterns to avoid, when **not** to use this pattern.
