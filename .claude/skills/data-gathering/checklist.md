# Data gathering checklist (report template)

**Agents:** Do not edit this file under `.claude/skills/data-gathering/`. Copy this structure **into your final reply** so the user can **scan every value** and reply with edits (e.g. “change screenshot 2 title to …”). A prose summary **does not** replace these tables.

**Rules:** Use the **exact** `##` / `###` headings below. Every table cell must contain the real value from this run (or `—` plus a one-word reason only if truly N/A). For **§2**, emit **one** five-row table per store file you wrote (`appstore.json` and/or `playstore.json`), each under `### appstore.json` or `### playstore.json`.

Replace placeholder dashes with real values. Add a **Source** column entry: file path (for scans) or `user` (for manual answers).

---

## 0. Config and scan roots

| Item | Value |
| --- | --- |
| `config.json` read from | — (always repo root `config.json` / `R/config.json`) |
| `ios_project_path` (raw → resolved) | — |
| `android_project_path` (raw → resolved) | — |
| `device_frame_type` (`iphone` \| `ipad` \| `phone` \| `tablet`) | — |
| `device_pack_path` | — |

---

## 1. Core listing fields

| Field | Value | Source |
| --- | --- | --- |
| App name | — | — |
| Subtitle | — | — |
| Description | — | — |
| Primary color | — | — |
| Secondary color | — | — |

---

## 2. Screenshot marketing copy (orders 1–5)

_App Store and Play Store share the same slot limits: title ≤30, subtitle ≤40, description ≤80 characters._

_Repeat the following block for each of `appstore.json` / `playstore.json` you actually wrote. Omit the block for a file you did not write._

### appstore.json

| Order | Title | Subtitle | Description |
| --- | --- | --- | --- |
| 1 | — | — | — |
| 2 | — | — | — |
| 3 | — | — | — |
| 4 | — | — | — |
| 5 | — | — | — |

### playstore.json

| Order | Title | Subtitle | Description |
| --- | --- | --- | --- |
| 1 | — | — | — |
| 2 | — | — | — |
| 3 | — | — | — |
| 4 | — | — | — |
| 5 | — | — | — |

_Character counts optional:_ beneath each table, note any cell within 5 characters of its limit.

---

## 3. Output folder confirmation

| Path (repo-relative) | Action | Status (written / updated / skipped) | Notes |
| --- | --- | --- | --- |
| `output/appstore.json` | — | — | — |
| `output/playstore.json` | — | — | — |

---

## 4. Gaps / follow-ups

_List empty required fields (URLs, emails, categories, ratings) or ambiguous scan results the user should confirm._
