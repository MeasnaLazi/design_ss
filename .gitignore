# Your app's screen captures — yours, not the project's.
datasource/screenshots/**

# Strips and their renders. No version history: do not overwrite one casually.
output/**

strip_editor/node_modules/
strip_editor/package-lock.json

# datasource/input/ (panel copy) and datasource/images/ (artwork for image
# layers) ARE tracked: a strip that references one must render from a fresh
# clone.

# macOS Finder droppings.
.DS_Store
