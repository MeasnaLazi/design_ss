# Your app's screen captures — yours, not the project's.
datasource/screenshots/**

# Renders are regenerable and land beside the strip that produced them.
strips/*/rendered/**
strips/.rendered/**

strip_editor/node_modules/
strip_editor/package-lock.json

# Everything else under strips/ IS tracked: strip.html and its images/ are the
# design, and a strip that references artwork must render from a fresh clone.

# macOS Finder droppings.
.DS_Store
