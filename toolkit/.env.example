# Copy to `.env` in this directory (`toolkit/`, next to `requirements.txt`) and adjust if needed.
# Used by `python toolkit/scripts/designer.py …`.
# Must stay on loopback (localhost / 127.0.0.1 / ::1); see `scripts/designer/client.py`.

DESIGNER_API_BASE=http://localhost:4713/__api/screenshot-designer
