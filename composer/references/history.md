# Run history

One line per design run, appended at the end of the run. The point is the
**last** line: a run reads it and must not repeat that concept.

This lives here rather than in the strip folder because a run *replaces*
`strips/<app-name>/` wholesale — anything recorded inside it is gone the moment
you re-run, which is exactly when the previous concept needs to be known.

Format:

```
<date> · <app> · <set rhythm> · <panel archetype> · <device> · <type> · <background/palette>
```

Example:

```
2026-08-09 · bio · hero-plus-support · type-over-device · framed/bottom-crop · above · radial dark + gold
```

Reading it back later also tells you what you have already tried for an app, and
what you have never once reached for.

---

2026-08-10 · bio · hero-plus-support · type-over-device (hero centered/larger panel 0) · framed front, alternating left/right bleed on 1-4 · above · dark ground (#0b0a08) + parchment ink + gold accent
2026-08-10 · bio · bookended (0/4 statements, 1-3 features) · type-dominant bookends + device-bleed-side alternating features · framed front, bottom/top-crop bookends + side-bleed features · beside device (split column) · dark ground (#0b0a08) + parchment ink + gold accent, book-folio decor
2026-08-10 · bio · continuous-canvas · device-over-type (full-bleed dimmed-top hero on panel 0) · bare rounded screenshot cards (no device frame), drop shadow, top-crop bleed with slight L/R drift · below card, hero overlapping dimmed top of screenshot · dark ground (#050403) + warm ivory ink + terracotta accent, spotlight mood, continuous "life timeline" line + chapter dot spanning all 5 panels as decor
