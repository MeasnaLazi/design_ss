# Run history

One line per design run, appended at the end of the run. The point is the
**last** line: a run reads it and must not repeat that concept.

This lives here rather than in the strip folder because a run *replaces*
`strips/<device>/` wholesale — anything recorded inside it is gone the moment
you re-run, which is exactly when the previous concept needs to be known.

Format:

```
<date> · <target> · <set rhythm> · <panel archetype> · <device treatment> · <type> · <background/palette>
```

Example:

```
2026-08-09 · iphone · hero-plus-support · type-over-device · framed/bottom-crop · above · radial dark + gold
```

Reading it back later also tells you what you have already tried for an app, and
what you have never once reached for.

---

2026-08-10 · bio · hero-plus-support · type-over-device (hero centered/larger panel 0) · framed front, alternating left/right bleed on 1-4 · above · dark ground (#0b0a08) + parchment ink + gold accent
2026-08-10 · bio · bookended (0/4 statements, 1-3 features) · type-dominant bookends + device-bleed-side alternating features · framed front, bottom/top-crop bookends + side-bleed features · beside device (split column) · dark ground (#0b0a08) + parchment ink + gold accent, book-folio decor
2026-08-10 · bio · continuous-canvas · device-over-type (full-bleed dimmed-top hero on panel 0) · bare rounded screenshot cards (no device frame), drop shadow, top-crop bleed with slight L/R drift · below card, hero overlapping dimmed top of screenshot · dark ground (#050403) + warm ivory ink + terracotta accent, spotlight mood, continuous "life timeline" line + chapter dot spanning all 5 panels as decor
2026-08-11 · bio · hero-plus-support · type-over-device hero (dominant flat, centered) + floating-card support (device incidental, rotated ±5°, alternating side) · framed front, drop-shadow depth, escaping crop-zoom cards (waveform/date-banner/AI-pills/swipe-actions) · above device (hero), beside-split alternating (support) · near-black ground (#0a0806) + warm parchment ink + amber accent, ember mood, feature-label pills + amber rule as decor
2026-08-11 · bio · alternating (device shift + tag/decor mirror panel to panel) · type-over-device, whole device visible (no crop), uniform scale across the set · framed front, drop-shadow, alternating ±90px horizontal shift · split (title top, subtitle bottom, device between) · near-black cool ground (#08090b) + cool paper-white ink + slate-blue accent, midnight mood, corner ring arc (alternating top-left/bottom-right) + numeric chapter tag as decor
2026-08-11 · bio · narrative (capture → speak → relive → perfect → control) · type-over-device dominant hero (panel 0) + floating-card features (1-4, UI fragments lifted and enlarged beside an incidental device) · framed front, bottom-crop bleed, alternating device side/scale · above device (hero), beside-column (features) · near-black ground (#050506) + warm-white ink + violet-pink-cyan gradient accent pulled from the app's own AI sparkle icon, neon mood, soft mesh glow + escaping UI cards (glowing rounded-corner cutouts) as decor
2026-08-11 · bio · hero-plus-support (panel 0 louder/cover, 1-4 quiet "chapter" support, 2-3 diverge further into two-device-overlap) · type-over-device dominant hero (panel 0, flat front) + single balanced device alternating side (1,4) + two-device-overlap balanced pair, dimmed/desaturated back + full-colour cropped front (2,3) · above device (chapter tag + title + subtitle stacked top-left across the set) · deep-water ground (#0a2830 teal-navy) + warm ivory ink + single brass/gold accent, deep-water mood, serif display title + chapter-number pill + thin brass rule as decor
2026-08-11 · bio · continuous-canvas · type-over-device, uniform stack (title/subtitle/rule at identical coords every panel), device the only thing that moves · framed front, bottom-crop bleed (-260px), horizontal sway centered/left/right/left/right across the set · above device (title+subtitle+rule fixed top-left) · near-black warm ground (#0b0805) + warm ivory ink + amber accent (#dfa24c), golden-hour mood with amber-to-violet drift across the full 6450px canvas, single hand-drawn ink "life-line" (echoes the app icon's swirl) running the full width behind the phones + amber ring chapter-number badge (01-05) as decor
2026-08-12 · bio · continuous-canvas (fixed header rule + numeral running full width across the set) · type-over-device single lead (0,1,3,4) + two-device-overlap (2 only, dimmed/desaturated back + full-colour cropped front) · framed front, alternating left/right bottom-crop bleed on singles, diagonal overlap pair on 2 · above device, title+subtitle block with generous clearance above the screen (screen starts almost at the frame edge on this pose, so a deeper text/device overlap collided with live status-bar UI and was pulled back for legibility) · flat near-black warm ground (#120e0c→#0a0807, identical every panel, no drift) + warm ivory ink + single terracotta accent (#c9765a), editorial mood, fine grain texture + plain numeral eyebrow (01-05) on a straight thin rule as decor
2026-08-12 · bio · escalating (calm split-panel open → dense/tilted close) · split-panel · framed front, increasing tilt (0°→7°) + increasing crop-bleed panel0→4, side alternates L/R · beside device (column, eyebrow+title+subtitle+rule stacked top) · pure-black ground (#050505/#0a0a0c split) + cool-white ink (#f4f6fa) + single electric-blue accent (#3d7fff), spotlight mood, escalating decor density (none → radial glow → geometric bars/dots → denser bars+particles). Note: initial rotation values (±9-10°) on panels 3-4 swung the tall device silhouette into the text column and obscured the subtitle — fixed by capping tilt at 6-7° and pulling the device further off-panel; worth remembering that a tall rotated rect's bounding-box swing scales with height, not width, so the safe-rotation range is tighter than it looks for a full-height phone.
2026-08-12 · bio · hero-plus-support (panel 0 louder full-bleed, 1-4 quiet) · full-bleed-screen (0, screenshot's own aspect nearly matches the panel so almost no crop) + device-over-type (1-4) · framed front, flat/no tilt, whole device visible (no crop), alternating ±70px left/right sway on 1-4 · overlaid on a top scrim (hero) / below the device (support, numeral + title + subtitle stacked) · near-black ground (#0a0a0c) + cool ivory ink (#eef1ec/#f4f6f2) + sage accent (#86a878), midnight mood (one soft radial highlight above each device, flat elsewhere) · decor: representational — a leaf silhouette echoing the app icon, as a small "BIO" brand chip on panel 0 and a large low-opacity terminal watermark on panel 4. First round left ~1200px of dead space below the support panels' subtitle (support text block was sized for a smaller/higher device); fixed by enlarging the device (930→960px) and pushing the whole device+type block down (top 140→220, text block +150px) so the bottom margin fell under the quarter-panel dead-space threshold.
2026-08-12 · bio · alternating (device side flips panel to panel; panel 0 kept as a lighter-crop hero within the same structure) · device-bleed-side · framed front, flat/no tilt, side-crop only (30% hero, 38-42% support), alternating left/right · beside device (kicker + serif title + subtitle column, vertically centered, opposite the device) · pure-black ground (#000) + warm ivory ink (#f2ede4) + dusty plum accent (#c496b2), overcast mood (flat, no glow anywhere) · decor: abstract — faint full-panel ruled-manuscript lines (1px, 5% opacity, 64px rhythm) + a thin accent-tinted vertical divider between the type column and the device on every panel.
2026-08-12 · bio · escalating (device tilt, crop depth and coverage grow panel1→4) · type-over-device hero (panel 0, dominant/centered) + floating-card support (1-4, balanced-scale dimmed/desaturated host device behind a full-colour cropped-region card escaping its top edge) · framed front, alternating left/right lean with side-crop, escalating tilt 2°→8°, dimmed host (opacity .55, grayscale 55%) · above device (title+subtitle stacked top-left, uniform position across the set) · pure-white ground (#ffffff) + near-black ink (#101010) + emerald accent (#0e9e5b), clinical mood · decor: representational — a small feature icon (waveform / clock / sparkle / stacked-layers) in a badge escaping each card's top-left corner, plus a leaf-swirl "BIO" brand mark on panel 0 only. First round bottom-anchored the host device (CSS `bottom:-Npx`) with an incidental (0.5-0.6×) scale, which left an ~800-900px fully empty band between the subtitle and the device/card — bottom-anchoring only crops the *bottom*, it does not pull content up toward the top when the device isn't tall enough to reach; fixed by top-anchoring a larger (0.72-0.85×) host device directly under the subtitle instead, which also fixed a badge text clip on panel 2 caused by unrelated crowding.
2026-08-13 · bio · bookended (0/4 claim-led hero/closer, still showing the app; 1-3 features) · type-dominant bookends (large serif claim, dominant device ~1260px, bottom-crop ~20%) + balanced-device features (whole device visible top-to-bottom, ~1100px, alternating left/right side-crop ~40%) · framed front, flat/no tilt throughout (no escalating angle this round) · above device on bookends (kicker+title+subtitle stacked top-left) / beside device on features (column opposite the device, alternating side) · screenshots as captured, no cropped-region cards · near-black ground (#0a0908) + warm ivory ink (#f2ede2) + single ink-indigo accent (#5a67d8), quiet-studio mood (flat, no glow anywhere) · decor: abstract — numeral folio (01/02/03) as ornament, hairline rule under bookend subtitles, a thin vertical "spine" line with dot terminators filling the gutter between text and device on features (echoes the book/journal metaphor without inventing any label text). First pass wrapped both bookend and feature titles to 3 lines, which collided with the subtitle directly below (line-height math not checked before positioning subtitle) — fixed by widening title columns so headlines wrap to 2 lines and pushing subtitle top past the measured title block height. Feature panels also started at ~42% coverage (device kept at a modest 980px width with 640px of empty vertical margin above/below it) — raised to ~48% by enlarging the device to 1100px and running it nearly the full panel height, but true 70%+ density wasn't reachable without either shrinking the text column until titles wrapped again or pushing crop past ~50%; left panels 1-3 deliberately airier/editorial than the denser bookends (82.7%/77.5%) rather than trade away legibility.
2026-08-13 · bio · hero-plus-support (panel 0 louder editorial cover, whole app visible; 1-4 quiet, alternating side) · type-over-device dominant hero (0, ~10% bottom-crop) + type-over-device balanced beside-column, whole device visible/no crop (1-4) · framed front, flat/no tilt, every device mounted on a warm parchment "photo-card" plane with drop shadow (new depth treatment — first run to use a mounting card rather than a bare shadow) · above device (hero, title only — app.md gave no subtitles this run) / beside device (features: short title stacked 2-3 words, alternating side) · warm parchment ground (#f7f0e0→#eee0c4) + espresso ink (#241b12) + terracotta accent (#b1543a), archival/parchment mood (first light-mode, non-stark-white run — every prior run was dark or clinical-white) + fine grain · decor: representational — one hand-drawn ink watermark per panel keyed to its claim (open book · microphone · hourglass · quill · stacked pages), plus a minor numeral-folio pagination (01-05) as abstract secondary ornament. Two rounds: first pass clipped "Complete Control" (panel 4) and part of "Perfect" (panel 3) past the panel/card edge — a single long word's rendered width exceeded its text-block width and, with no manual line break, overflowed the box into the panel edge or the card sitting later in DOM order; fixed by shrinking font-size and widening the available column rather than truncating the copy. Also tightened ~170px of dead space above the device/title block in the four feature panels, moving the card+device+title group up as a unit.
2026-08-14 · BAC Digital Banking (bank) · alternating (device side flips left/right panel to panel) · device-bleed-side · framed front, flat/no tilt, ~22% of the device hidden off the panel's outer edge (bank UI's full-width labels made the usual 30-50% bleed cut through account/label text, so the crop was pulled back), plain drop shadow, no mounting card · beside device (kicker "BAC" + rule + title + subtitle column, vertically centered opposite the device rather than pinned top) · pure-white ground (#ffffff) + near-black ink (#19161a) + BAC's own maroon accent (#8a2432, pulled straight from the app's brand rather than invented), clinical mood, first fully light run with no grain/texture at all · decor: abstract — a single oversized numeral (1-5) bleeding off the panel's outer top corner at ~5.5% opacity, echoing the folio-tag convention but scaled up into a watermark. First pass top-anchored the type block at a fixed y, which read fine alone but left ~55% total coverage with a ~2000px fully empty column below the subtitle once the device (only 73% of panel height) sat in the other half of the panel; fixed by vertically centering the kicker/title/subtitle block against the device's occupied vertical span instead of pinning it near the top, which reads as deliberate air rather than truncated content — coverage stayed ~55% but the panel no longer looks like it ran out of copy. Also: Apple's rule against login/splash as the *lead* screenshot pushed the Login capture (used for its FDIC/Face ID content) to panel 4 rather than panel 0.
2026-08-14 · BAC Digital Banking (bank) · narrative (everyday banking → track → move → expand → login/security close) · floating-card support (1-3: a cropped UI-fragment card escapes above a dimmed/desaturated incidental-to-balanced host device) + type-over-device hero (0) + balanced beside-device closer (4, no card, whole screen visible) · framed front, alternating tilt ±3-4°, host devices grayscale(0.7)+brightness(0.75)+opacity(0.55) on support panels · above device (hero) / beside-device column alternating left↔right (support + closer) · near-black ground (#150f12→#0a0708) + warm ivory ink (#f6efe7) + BAC's own maroon accent (#b5324f), spotlight mood (soft radial glow on hero/closer only) · decor: abstract — persistent top-left "BAC" kicker + rule every panel, large faint numeral folio (01-05) at whichever bottom corner sits under the type column; first fully abstract-decor run for this app after the previous run's representational icon badges. Two real bugs this round, both from bad estimates rather than the render: (1) title columns were sized at 430-460px/86px assuming a 2-line wrap, but Georgia bold at that width actually wraps 3 lines for headlines like "Every Transaction, Tracked" — the fixed-offset subtitle then landed inside the title's third line. Fixed by widening to 460px/72px and using a flat 265-278px title→subtitle gap sized for the 3-line case rather than a per-panel guess. (2) panel 4's closer device was sized to 950px width without checking that its right edge (left 90 + 950 = 1040) ran 280px past where the text column started (760) — pure arithmetic that wasn't checked before rendering. Fixed by shrinking/repositioning the device relative to the text column with an explicit 40px clearance rule applied to every device↔text pairing after. Also caught late: initial support-panel devices were sized incidental (620px) and top-anchored high, which — combined with the type block also sitting low — left ~43% of each support panel as a genuine full-width empty band below the device; fixed by enlarging the devices to ~1080px (crossing into balanced/dominant territory even though a card sits in front) and moving the type block up to start right under the kicker, which raised coverage from ~49-50% to 66-70% without shrinking the card treatment.
2026-08-14 · BAC Digital Banking (bank) · hero-plus-support (panel 0 dominant and centered, 1-4 quieter, uniform layout that alternates left/right horizontal offset) · type-over-device throughout (dominant flat device on 0, balanced-scale on 1-4) · framed front, flat/no tilt, modest bottom-crop (device runs slightly past the panel foot on every panel, no side-bleed this round), plain drop shadow · above device (eyebrow icon+label, title, subtitle stacked top-left, same column on every panel) · warm off-white gradient ground (#f8f6f2→#ece7df) + near-black ink (#17130f) + IBC/BAC maroon accent (#bb010c, from `app.md`'s `theme`), overcast mood (flat light, no glow) · decor: representational — a small stroke-icon badge per panel matched to its claim (bar-chart/Accounts, swap-arrows/Transfers, calendar-check/Bill Pay, camera/Deposit, shield-check/Security), first representational run for this app after the previous run's abstract numeral watermark. One clean round: schema and render both passed with zero problems on the first attempt; the only fix was restyling the panel-0 icon from a filled glyph to the same stroke weight as the other four for visual consistency. Coverage measured 69.5-73.8% across all five panels, in range without forcing it. Note for next time: this app's `app.md` H1 names it "BAC Digital Banking" but every screenshot and the `## About` body copy say "IBC" — flagged to the user rather than guessed at; this run followed the H1 per the skill's naming rule and kept the previous run's "BAC" kicker precedent.
